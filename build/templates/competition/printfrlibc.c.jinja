{{header}}

{{ignore_me}}

int vuln()
{
  char s[{{buffersize}}];
  printf("{{greeting}} >>>\n");
  fgets(s, {{buffersize}}, stdin);
  printf("\n<<< Leak: ");
  printf(s, {{buffersize}});
  printf("\n {{prompt}} >>>");
  read(0, s, {{buffersize+0x100}});
  return 0;
}

{{main}}

