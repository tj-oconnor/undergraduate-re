{{header}}

{{ignore_me}}

void constrained_win(int a, int b) {
  {{win_char}}

  printf("{{rand_word}}\n");
  if ((a=={{rand_val1}}) && (b=={{rand_val2}})) {
    system(cmd);
  }
}

int vuln()
{
  char buf[{{buffersize}}];
  printf("{{greeting}} >>>\n");

  uint64_t target=(uint64_t)(&__libc_csu_init)+90;
  {{define_target}}

  gets(buf);

  {{check_target}}


  printf("<<< {{closing}}");

}

{{main}}

