{{header}}

{{ignore_me}}

{{display_flag}}

int validate (int x, int y, int z) {
    if ((z/x) > (z/y)) {
        if (((z%x) + (z%y))==4) {
            if (z==(10003%4000)) {
                if ((y%2)==1) {
                    if ((z%2==1)) {
                        if ((x+y)<18) {
                            if (y==((z%y)+2*x)) {
                                return 1;
                            }
                        }
                    }
                }
            }
         }
    }
    return 0;
}

int challenge () {
    int x;
    int y;
    int z;

    int valid;
    printf("Enter x >>> \n");
    scanf("%d", &x);
    printf("Enter y >>> \n");
    scanf("%d", &y);
    printf("Enter z >>> \n");
    scanf("%d", &z);

    if (validate(x,y,z)) {
        display_flag("flag.txt");
    } else {
        printf("<<< You failed. Try Again \n");
    }

    return 0;
}

{{main}}